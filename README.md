# Global CPI Detection

This repository now contains two tracks:

1. `src/cpis/` and `cpis.py`: newer modular pipeline.
2. Legacy MMDetection scripts (paper-inspired experimentation).

## Start Here

Use these docs for the current clean structure, active outputs, and archive locations:

- `START_HERE.md`
- `WORKSPACE_INDEX.md`

## Current CLI

```bash
python cpis.py --help
```

## Note on Legacy Docs

Older status/readme files were archived during cleanup to:

- `archive/2026-03-09_cleanup/legacy_docs/`
 
